# Node.js
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log*
bun.lockb
# Keep bun.lock as it is text-based (if user wants, but usually standard) - wait, bun.lock is text based in newer versions but let's stick to standard node ignores first.
# Actually bun.lock is important to commit usually. but if it's binary it's bun.lockb.
# The user has bun.lock.

# Build output
dist/
out/
build/

# System
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vite/Vitest code coverage
coverage/

# Misc
.tshy-build
